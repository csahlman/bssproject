<%= simple_form_for [vote.voteable, vote], html: { id: "upvote", 
  class: 'btn btn-small btn-success' }, remote: true do |f| %>
  <%= f.hidden_field :vote_value, value: 1 %>
  <%= f.button :submit, "Upvote" %>
<% end %>