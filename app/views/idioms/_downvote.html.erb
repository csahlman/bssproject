<%= simple_form_for [vote.voteable, vote], html: { id: "downvote", 
  class: 'btn btn-small' }, remote: true do |f| %>
  <%= f.hidden_field :vote_value, value: -1 %>
  <%= f.button :submit, "Downvote" %>
<% end %>