 <%= simple_form_for [vote.voteable.idiom, vote.voteable, vote], 
    html: { id: "tag_upvote_#{vote.id}"}, remote: true do |f| %>
   <%= f.hidden_field :vote_value, value: 1 %>
   <%= f.button :submit, '+', id: "tag_upvote_button_#{vote.id}", 
       class: vote.vote_value == 1 ? 'btn-success' : "" %>
 <% end %>  
