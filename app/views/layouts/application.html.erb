<!DOCTYPE html>
<html lang="en">

  <%= render 'layouts/header' %>

  <body>

    <%= render 'layouts/nav_links' %>

    <div class="container">
      <%= render 'shared/display_announcements' %>
      <%= render 'shared/display_flash' %>
        <div class="row">
          <div class="span9">
            <%= yield %>
          </div>
          <% if content_for?(:left_frame) %>
          <div class="span5">
            <%= yield(:left_frame) %>
          </div>
          <% end %>          
          <% if content_for?(:right_frame) %>
          <div class="span4">
            <%= yield(:right_frame)%>
          </div>
          <% end %>
          <%= render 'layouts/sidebar' if content_for?(:sidebar) %>
          <% if content_for?(:bottom_frame) %>
          <div class="span9">
            <%= yield(:bottom_frame) %>
          </div>
          <% end %>
        </div><!--/row-->

      <footer>
        <p>&copy; Boston Startup School 2012</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <%= yield(:scripts) if content_for?(:scripts) %>
    <%= debug(params) if Rails.env.development? %>

  </body>
</html>
